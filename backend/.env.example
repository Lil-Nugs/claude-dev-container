# Claude Dev Container - Backend Environment Variables
# Copy this file to .env and customize as needed
#
# The backend uses pydantic-settings, which reads from environment variables
# with the same name as the settings (case-insensitive, uppercase preferred)

# =============================================================================
# Workspace Configuration
# =============================================================================

# Path to the directory containing your projects
# The backend will scan this directory for git repositories
# Default: ~/projects (user's home directory + projects)
WORKSPACE_PATH=~/projects

# =============================================================================
# API Configuration
# =============================================================================

# Host to bind the FastAPI server to
# Use 0.0.0.0 to allow external connections, 127.0.0.1 for localhost only
# Default: 0.0.0.0
API_HOST=0.0.0.0

# Port for the FastAPI server
# Default: 8000
API_PORT=8000

# =============================================================================
# Docker Configuration
# =============================================================================

# Path to the Docker socket
# Used for container management (creating/managing dev containers)
# Default: /var/run/docker.sock
DOCKER_SOCKET=/var/run/docker.sock
